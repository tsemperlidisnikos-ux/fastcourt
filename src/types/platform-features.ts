export interface PlatformFeaturesConfig {
  /** Library preview "Similar plays" + Play DNA matching in suggestions. */
  similarPlays: boolean;
  /** Designer right-sidebar Coach tab (analyze / Ask AI). */
  designerCoach: boolean;
}
