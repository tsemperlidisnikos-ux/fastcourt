"use client";

import { DrawLibraryView } from "@/components/library/DrawLibraryView";

/** Dedicated Counter Library tab — defense coverage plays only. */
export function CountersLibraryView() {
  return <DrawLibraryView variant="counters" />;
}
